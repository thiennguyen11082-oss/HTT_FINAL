node_modules
dist
.shots

# Never commit mail credentials — SMTP_USER / SMTP_PASS / CONTACT_TO live in
# Vercel environment variables only.
.env
.env.*
!.env.example

*.local
.DS_Store
.vercel

# TypeScript incremental build state
tsconfig.tsbuildinfo
